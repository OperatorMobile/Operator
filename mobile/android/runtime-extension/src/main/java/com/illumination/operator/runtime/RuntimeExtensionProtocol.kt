package com.illumination.operator.runtime

object RuntimeExtensionProtocol {
    const val ACTION_BIND = "com.illumination.operator.runtime.BIND"
    const val BIND_PERMISSION = "com.illumination.operator.runtime.permission.BIND"

    const val MSG_CAPABILITIES = 1
    const val MSG_EXEC = 2

    const val KEY_REQUEST_ID = "requestId"
    const val KEY_JSON = "json"
}
